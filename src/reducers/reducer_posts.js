import { FETCH_POSTS } from '../actions/index';

const INITIAL_STATE = {all: [], posts: null};


export default functions(state = INITIAL_STATE, action) {
  switch(action.type) {
    case FETCH_POSTS:
       
    defalt:
      return state;
  }
}
